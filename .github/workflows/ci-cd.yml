name: CI and Docker Hub Publish

on:
  pull_request:
  push:
    branches:
      - "**"
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

env:
  DOCKER_IMAGE: kyliroco/ssda2026

jobs:
  runner-tests:
    name: Runner Tests (Docker Image)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build web image for tests
        run: docker build -t web-test:${{ github.sha }} ./web

      - name: Create test network
        run: docker network create web-test-net

      - name: Start PostgreSQL test container
        run: |
          set -euo pipefail
          docker run -d \
            --name db-test \
            --network web-test-net \
            -e POSTGRES_DB=generate_or_not \
            -e POSTGRES_USER=game_user \
            -e POSTGRES_PASSWORD=change_me_now \
            postgres:16-alpine

      - name: Wait for PostgreSQL readiness
        run: |
          set -euo pipefail
          for _ in {1..30}; do
            if docker exec db-test pg_isready -U game_user -d generate_or_not >/dev/null 2>&1; then
              exit 0
            fi
            sleep 2
          done
          echo "PostgreSQL did not become ready in time."
          exit 1

      - name: Start test container from image
        run: |
          set -euo pipefail
          docker run -d \
            --name web-test \
            --network web-test-net \
            -p 6767:6767 \
            -e APP_PORT=6767 \
            -e DATABASE_URL=postgresql://game_user:change_me_now@db-test:5432/generate_or_not \
            web-test:${{ github.sha }}

      - name: Check image endpoints
        run: |
          set -euo pipefail

          for _ in {1..30}; do
            if curl -fsS http://localhost:6767/health >/dev/null; then
              break
            fi
            sleep 2
          done

          curl -fsS http://localhost:6767/ >/dev/null
          health_payload="$(curl -fsS http://localhost:6767/health)"
          echo "$health_payload"
          echo "$health_payload" | grep -q '"app":"up"'

      - name: Show container status
        if: always()
        run: docker ps -a --filter name=web-test --filter name=db-test

      - name: Show logs on failure
        if: failure()
        run: |
          docker logs web-test || true
          docker logs db-test || true

      - name: Cleanup test resources
        if: always()
        run: |
          docker rm -f web-test || true
          docker rm -f db-test || true
          docker network rm web-test-net || true

  publish-dockerhub:
    name: Publish Web Image to Docker Hub
    runs-on: ubuntu-latest
    needs: runner-tests
    if: github.event_name != 'pull_request' && (github.ref_name == 'main' || github.ref_name == 'master')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate Docker Hub secrets
        run: |
          if [ -z "${{ secrets.DOCKERUSER }}" ] || [ -z "${{ secrets.DOCKERPASS }}" ]; then
            echo "Secrets DOCKERUSER and DOCKERPASS are required."
            exit 1
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERUSER }}
          password: ${{ secrets.DOCKERPASS }}

      - name: Extract image metadata
        id: metadata
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_IMAGE }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-
            type=ref,event=branch
            type=ref,event=tag

      - name: Build and push web image
        uses: docker/build-push-action@v6
        with:
          context: ./web
          file: ./web/Dockerfile
          push: true
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
